#Cozloff
========

The Cozloff is a Rust program.

### Quick Start

Run shell:
```bash
docker compose build
docker compose run --rm cozloff bash
cargo run
```

Open Up Scalar: 
```bash
http://127.0.0.1:8080/network
```

View wave function:
```bash
.venv/bin/rerun wave.rrd
```
